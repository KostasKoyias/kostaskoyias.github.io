{
  "name": "kostaskoyias.github.io",
  "version": "1.0.0",
  "description": "Minimal portfolio page",
  "main": "build/index.js",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "watch": "npx babel --watch js --out-dir build --presets react-app/prod",
    "dev": "concurrently --kill-others \"npm run watch\" \"npm start\"",
    "build": "npx babel js --out-dir build --presets react-app/prod",
    "start": "npx tiny-server"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com-KostasKoyias:KostasKoyias/kostaskoyias.github.io.git"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-react-app": "^3.1.2",
    "tiny-server": "^1.1.0"
  },
  "devDependencies": {
    "concurrently": "^6.5.1"
  }
}
